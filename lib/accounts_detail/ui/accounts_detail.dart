import 'package:flutter/material.dart';

class AccountsDetail extends StatelessWidget {
  const AccountsDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
